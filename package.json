{
  "name": "train-station-chatops",
  "version": "0.1.0",
  "private": true,
  "description": "Standalone ChatOps PWA for train station timetable & holiday management via Cloudflare Pages and Gemini API",
  "type": "module",
  "scripts": {
    "dev": "wrangler pages dev public",
    "build": "node -e 'console.log(\"Build complete: static PWA assets in public/ require no compilation.\")'",
    "test": "node --test",
    "test:unit": "node --test tests/unit/",
    "test:e2e": "node --test tests/e2e/",
    "lint": "git ls-files '*.js' | xargs -r -n 1 node --check"
  },
  "keywords": [
    "chatops",
    "pwa",
    "cloudflare-pages",
    "gemini-api",
    "kiosk",
    "timetable",
    "belgium",
    "genval"
  ],
  "author": "LoupiBe",
  "license": "MIT",
  "devDependencies": {
    "wrangler": "^4.0.0"
  },
  "dependencies": {},
  "engines": {
    "node": ">=20.0.0"
  }
}
